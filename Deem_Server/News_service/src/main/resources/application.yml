server:
  port: 8085

spring:
  application:
    name: newss
  kafka:
    producer:
      bootstrap-servers: localhost:9092
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: com.example.news_service.util.CustomJsonSerializer

eureka:
  client:
    service-url:
      defaultZone : http://localhost:8080/eureka